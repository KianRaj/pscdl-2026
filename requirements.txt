# PSCDL 2026 — Persistent Scene Change Detection pipeline
# Tested with Python 3.10 (conda env "depth-pro"), CUDA 12.1, single NVIDIA GPU (>=8 GB).
#
# PyTorch built for CUDA 12.1 — install from the official index:
#   pip install torch==2.4.0 torchvision==0.19.0 --index-url https://download.pytorch.org/whl/cu121
torch==2.4.0
torchvision==0.19.0

# Core CV / numerics
numpy==1.26.4
opencv-python==4.11.0.86
Pillow==10.4.0
scipy==1.13.1

# Detection / backbone
ultralytics==8.4.41          # YOLOv8m person filter
timm==1.0.22                 # Siamese ResNet50 backbone

# SAM3 segmentation (Meta Segment Anything 3) + HF stack
sam3==0.1.0
transformers==4.57.6
huggingface-hub==0.36.2

# Utilities / plotting
matplotlib==3.9.4
tqdm==4.67.1

# CLIP semantic false-positive gate (v6) — OpenAI CLIP ViT-B/32
ftfy
regex
clip @ git+https://github.com/openai/CLIP.git
